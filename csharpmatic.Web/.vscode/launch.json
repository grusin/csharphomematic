{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch http://localhost:3000",
            "type": "firefox",
            "request": "launch",           
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}"
        }
    ]
}